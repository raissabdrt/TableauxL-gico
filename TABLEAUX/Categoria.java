package TABLEAUX;

/*
@iza.francine
 * Essa classe é um enum (enumeração) e tem por objetivo armazenar as constantes (dados fixos)
 * utilizados, uma vez que Átomo, Negação, E, Ou e Implicação serão atributos imutáveis na 
 * construção do código, mantendo a organização e legibilidade. Essa classe será utilizada na 
 * classe Formula. 
 */

public enum Categoria {

    ATOMO,
    NEGACAO,
    E,
    OU,
    IMPLICACAO

}
